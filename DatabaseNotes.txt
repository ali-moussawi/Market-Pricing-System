1.must be add category = "NO CATEGORY".
2.must be add role ="Admin".
3.must be all permissions added in database(same name in picture).
4.must have user with role admin (change email in code to the new email(allusers + dashboard users));