1.must be add category = "NO CATEGORY".
2.must be add role ="Admin" (admin has allpermissions).
4.must be all permissions added in database(same name in picture).
5.must have user with role admin (change email in code to the new email(allusers sp +dashboard users));

code notes:
1-update sp(new email) that fetch all users.
2-update dashboard(new email) to not fetch admin email.